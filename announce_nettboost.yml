name: Announce new NettBoost release

on:
  push:
    branches: [main]
    paths:
      - "FixCore_NettBoost_Setup.exe"

jobs:
  announce:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Read nettboost_version.json and post to Discord
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          VERSION=$(jq -r '.version' nettboost_version.json)
          CHANGELOG=$(jq -r '.changelog' nettboost_version.json)
          DOWNLOAD_URL=$(jq -r '.download_url' nettboost_version.json)
          TYPE=$(jq -r '.type // "update"' nettboost_version.json)

          case "$TYPE" in
            update)       EMOJI="🚀"; LABEL="Update";       COLOR=2201331  ;;
            bug_fix)      EMOJI="🐛"; LABEL="Bug Fix";      COLOR=3066993  ;;
            feature)      EMOJI="✨"; LABEL="New Feature";  COLOR=9109247  ;;
            maintenance)  EMOJI="🛠️"; LABEL="Maintenance";  COLOR=15105570 ;;
            important)    EMOJI="⚠️"; LABEL="Important";    COLOR=15158332 ;;
            *)            EMOJI="📢"; LABEL="Announcement"; COLOR=2201331  ;;
          esac

          curl -sf -X POST "$DISCORD_WEBHOOK_URL" \
            -H "Content-Type: application/json" \
            -d @- <<EOF
          {
            "content": "@everyone",
            "embeds": [
              {
                "title": "${EMOJI} NettBoost (Lagster) ${VERSION} — ${LABEL}",
                "description": "${CHANGELOG}\n\n[⬇️ Download here](${DOWNLOAD_URL})",
                "color": ${COLOR},
                "footer": { "text": "NettBoost • ${LABEL} • ${VERSION}" },
                "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
              }
            ]
          }
          EOF
